[project]
name = "tasks"
version = "4.0.2"
description = "A simple command-line task list"
authors = [
    {name = "Marcus Kazmierczak", email = "marcus@mkaz.com"}
]
license = "MIT"
readme = "readme.md"
keywords = ["todo"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Topic :: Utilities",
]
requires-python = ">=3.12"
dependencies = [
    "appdirs>=1.4",
    "rich>=13.3",
    "prompt_toolkit>=3.0",
]

[dependency-groups]
dev = [
    "pytest>=7.0",
    "pytest-mock>=3.10.0",
]

[project.scripts]
tasks = "tasks.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-ra"
pythonpath = "tasks"
testpaths = [
    "tests",
]

[tool.ruff]
target-version = "py312"
